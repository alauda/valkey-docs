---
weight: 30
i18n:
  title:
    en: Kubernetes APIs
---

# Kubernetes APIs

Kubernetes admission validates resource shape, but schema acceptance alone does
not establish product support. In particular, the high-level version enum is
broader than the server images packaged for 2.0.0.

<Overview overviewHeaders={[]} />
